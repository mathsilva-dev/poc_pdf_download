import 'package:flutter/material.dart';

class PaymentCardDownloadedScreen extends StatelessWidget {
  const PaymentCardDownloadedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Payment Card\nDownloaded', textAlign: TextAlign.center),
      ),
    );
  }
}
